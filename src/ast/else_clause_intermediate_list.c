/**
 * @file else_clause_intermediate_list.c
 *
 * @brief $$ = createElseClauseIntermediateList();
 *
 * NOTE: This file is automatically generated by the boilerplate script.
 */
#include "memory.h"
#include "ast.h"

/**
 * @brief ElseClauseIntermediateList* createElseClauseIntermediateList(ElseClause* else_clause,
		ElseClauseIntermediateList* else_clause_intermediate_list)
 *
 */
ElseClauseIntermediateList* createElseClauseIntermediateList(ElseClause* else_clause,
		ElseClauseIntermediateList* else_clause_intermediate_list) {

#ifdef ENABLE_TRACE
    printf("parser: createElseClauseIntermediateList\\n");
#endif /* ENABLE_DUMP */

    ElseClauseIntermediateList* ptr = _alloc_ds(ElseClauseIntermediateList);
    initAst(&ptr->ast, NULL, AST_ELSE_CLAUSE_INTERMEDIATE_LIST);
    ptr->else_clause = else_clause;
    ptr->else_clause_intermediate_list = else_clause_intermediate_list;


    return ptr;
}

/**
 * @brief Execute pass 1 analysis for ElseClauseIntermediateList data structure.
 *
 */
AstResult pass1ElseClauseIntermediateList(ElseClauseIntermediateList* ptr) {

#ifdef ENABLE_TRACE
    printf("parser: pass1ElseClauseIntermediateList\\n");
#endif /* ENABLE_DUMP */

    if(ptr != NULL) {
        // perform the pass 1 routines

        // traverse the data structure items
        if(ptr->else_clause != NULL) {
            AstResult res = pass1ElseClause(ptr->else_clause);
            if(res != AST_RES_OK)
                return res;
        }

        if(ptr->else_clause_intermediate_list != NULL) {
            AstResult res = pass1ElseClauseIntermediateList(ptr->else_clause_intermediate_list);
            if(res != AST_RES_OK)
                return res;
        }

    }

    return AST_RES_OK;
}

/**
 * @brief Execute pass 2 analysis for ElseClauseIntermediateList data structure.
 *
 */
AstResult pass2ElseClauseIntermediateList(ElseClauseIntermediateList* ptr) {

#ifdef ENABLE_TRACE
    printf("parser: pass2ElseClauseIntermediateList\\n");
#endif /* ENABLE_DUMP */

    if(ptr != NULL) {
        // perform the pass 2 routines

        // traverse the data structure items
        if(ptr->else_clause != NULL) {
            AstResult res = pass2ElseClause(ptr->else_clause);
            if(res != AST_RES_OK)
                return res;
        }

        if(ptr->else_clause_intermediate_list != NULL) {
            AstResult res = pass2ElseClauseIntermediateList(ptr->else_clause_intermediate_list);
            if(res != AST_RES_OK)
                return res;
        }

    }

    return AST_RES_OK;
}

/**
 * @brief Execute pass 3 analysis for ElseClauseIntermediateList data structure.
 *
 */
AstResult pass3ElseClauseIntermediateList(ElseClauseIntermediateList* ptr) {

#ifdef ENABLE_TRACE
    printf("parser: pass3ElseClauseIntermediateList\\n");
#endif /* ENABLE_DUMP */

    if(ptr != NULL) {
        // perform the pass 3 routines

        // traverse the data structure items
        if(ptr->else_clause != NULL) {
            AstResult res = pass3ElseClause(ptr->else_clause);
            if(res != AST_RES_OK)
                return res;
        }

        if(ptr->else_clause_intermediate_list != NULL) {
            AstResult res = pass3ElseClauseIntermediateList(ptr->else_clause_intermediate_list);
            if(res != AST_RES_OK)
                return res;
        }

    }

    return AST_RES_OK;
}

/**
 * @brief Emit final output for ElseClauseIntermediateList data structure.
 *
 */
AstResult emitElseClauseIntermediateList(ElseClauseIntermediateList* ptr) {

#ifdef ENABLE_TRACE
    printf("parser: emitElseClauseIntermediateList\\n");
#endif /* ENABLE_DUMP */

    if(ptr != NULL) {
        // perform the emitter routines

        // traverse the data structure items
        if(ptr->else_clause != NULL) {
            AstResult res = emitElseClause(ptr->else_clause);
            if(res != AST_RES_OK)
                return res;
        }

        if(ptr->else_clause_intermediate_list != NULL) {
            AstResult res = emitElseClauseIntermediateList(ptr->else_clause_intermediate_list);
            if(res != AST_RES_OK)
                return res;
        }

    }

    return AST_RES_OK;
}

/**
 * @brief Destroy a ElseClauseIntermediateList data structure.
 *
 */
AstResult destroyElseClauseIntermediateList(ElseClauseIntermediateList* ptr) {

#ifdef ENABLE_TRACE
    printf("parser: destroyElseClauseIntermediateList\\n");
#endif /* ENABLE_DUMP */

    if(ptr != NULL) {
        // destroy these data elements

        // traverse the data structure items
        if(ptr->else_clause != NULL) {
            AstResult res = destroyElseClause(ptr->else_clause);
            if(res != AST_RES_OK)
                return res;
        }

        if(ptr->else_clause_intermediate_list != NULL) {
            AstResult res = destroyElseClauseIntermediateList(ptr->else_clause_intermediate_list);
            if(res != AST_RES_OK)
                return res;
        }

        _free(ptr);
    }

    return AST_RES_OK;
}

/**
 * @brief Dump ElseClauseIntermediateList data type and progress the tree.
 *
 */
#ifdef ENABLE_DUMP
AstResult dumpElseClauseIntermediateList(ElseClauseIntermediateList* ptr) {

    //(void)ptr;
    if(ptr != NULL) {
        dumpAst(&ptr->ast);
        // dump this item

        // traverse the data structure items
        if(ptr->else_clause != NULL) {
            AstResult res = dumpElseClause(ptr->else_clause);
            if(res != AST_RES_OK)
                return res;
        }

        if(ptr->else_clause_intermediate_list != NULL) {
            AstResult res = dumpElseClauseIntermediateList(ptr->else_clause_intermediate_list);
            if(res != AST_RES_OK)
                return res;
        }

    }

    return AST_RES_OK;
}
#endif /* ENABLE_DUMP */
