/**
 * @file class_name.c
 *
 * @brief $$ = createClassName();
 *
 * NOTE: This file is automatically generated by the boilerplate script.
 */
#include "memory.h"
#include "ast.h"

/**
 * @brief ClassName* createClassName(const char* symbol,
		ClassNameParent* class_name_parent)
 *
 */
ClassName* createClassName(const char* symbol,
		ClassNameParent* class_name_parent) {

#ifdef ENABLE_TRACE
    printf("parser: createClassName\\n");
#endif /* ENABLE_DUMP */

    ClassName* ptr = _alloc_ds(ClassName);
    initAst(&ptr->ast, NULL, AST_CLASS_NAME);
    ptr->symbol = symbol;
    ptr->class_name_parent = class_name_parent;

    return ptr;
}

/**
 * @brief Execute pass 1 analysis for this data structure.
 *
 */
AstResult pass1ClassName(ClassName* ptr) {

    if(ptr != NULL) {
        // perform the pass 1 routines

        // traverse the data structure items
        if(ptr->class_name_parent != NULL) {
            AstResult res = pass1ClassNameParent(ptr->class_name_parent);
            if(res != AST_RES_OK)
                return res;
        }

    }
    return AST_RES_OK;
}

/**
 * @brief Execute pass 2 analysis for this data structure.
 *
 */
AstResult pass2ClassName(ClassName* ptr) {

    if(ptr != NULL) {
        // perform the pass 2 routines

        // traverse the data structure items
        if(ptr->class_name_parent != NULL) {
            AstResult res = pass2ClassNameParent(ptr->class_name_parent);
            if(res != AST_RES_OK)
                return res;
        }

    }
    return AST_RES_OK;
}

/**
 * @brief Execute pass 3 analysis for this data structure.
 *
 */
AstResult pass3ClassName(ClassName* ptr) {

    if(ptr != NULL) {
        // perform the pass 3 routines

        // traverse the data structure items
        if(ptr->class_name_parent != NULL) {
            AstResult res = pass3ClassNameParent(ptr->class_name_parent);
            if(res != AST_RES_OK)
                return res;
        }

    }
    return AST_RES_OK;
}

/**
 * @brief Emit final output for this data structure.
 *
 */
AstResult emitClassName(ClassName* ptr) {

    if(ptr != NULL) {
        // perform the emitter routines

        // traverse the data structure items
        if(ptr->class_name_parent != NULL) {
            AstResult res = emitClassNameParent(ptr->class_name_parent);
            if(res != AST_RES_OK)
                return res;
        }

    }
    return AST_RES_OK;
}

/**
 * @brief void destroyClassName(ClassName* ptr)
 *
 */
AstResult destroyClassName(ClassName* ptr) {

    if(ptr != NULL) {
        // destroy these data elements

        // traverse the data structure items
        if(ptr->class_name_parent != NULL) {
            AstResult res = destroyClassNameParent(ptr->class_name_parent);
            if(res != AST_RES_OK)
                return res;
        }

        _free(ptr);
    }
    return AST_RES_OK;
}

/**
 * @brief Dump the data type and progress the tree.
 *
 */
#ifdef ENABLE_DUMP
AstResult dumpClassName(ClassName* ptr) {

    //(void)ptr;
    if(ptr != NULL) {
        dumpAst(&ptr->ast);
        // dump this item

        // traverse the data structure items
        if(ptr->class_name_parent != NULL) {
            AstResult res = dumpClassNameParent(ptr->class_name_parent);
            if(res != AST_RES_OK)
                return res;
        }

    }
    return AST_RES_OK;
}
#endif /* ENABLE_DUMP */
