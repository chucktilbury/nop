/**
 * @file {{filename}}
 *
 */
#ifndef _{{lockname}}_H_
#define _{{lockname}}_H_

#include "ast.h"

typedef enum {
    {{lockname}}_OK,
    {{lockname}}_ERROR,
} {{typename}}Type;

struct {{strucname}} {
    Ast ast;
    {{typename}}Type type;
{{strucdef-}}
};

{{typename}}* create{{typename}}({{paradecl}});
void destroy{{typename}}({{typename}}*);

#ifdef ENABLE_DUMP
void dump{{typename}}({{typename}}* ptr);
#endif

#endif

