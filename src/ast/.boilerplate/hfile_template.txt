/**
 * @file {{filename}}
 *
 * NOTE: This file is generated by the boilerplate functionality.
 */
#ifndef _{{lockname}}_H_
#define _{{lockname}}_H_

#include "ast.h"

typedef enum {
    {{lockname}}_OK,
    {{lockname}}_ERROR,
} {{typename}}Type;

struct {{strucname}} {
    Ast ast;
    {{typename}}Type type;
{{strucdef-}}
};

{{typename}}* create{{typename}}({{paradecl}});
AstResult pass1{{typename}}({{typename}}*);
AstResult pass2{{typename}}({{typename}}*);
AstResult pass3{{typename}}({{typename}}*);
AstResult emit{{typename}}({{typename}}*);
AstResult destroy{{typename}}({{typename}}*);

#ifdef ENABLE_DUMP
AstResult dump{{typename}}({{typename}}* ptr);
#endif

#endif

