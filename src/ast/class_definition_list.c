/**
 * @file class_definition_list.c
 *
 * @brief $$ = createClassDefinitionList();
 *
 * NOTE: This file is automatically generated by the boilerplate script.
 */
#include "memory.h"
#include "ast.h"

/**
 * @brief ClassDefinitionList* createClassDefinitionList(ClassDefinitionItem* class_definition_item,
		ClassDefinitionList* class_definition_list)
 *
 */
ClassDefinitionList* createClassDefinitionList(ClassDefinitionItem* class_definition_item,
		ClassDefinitionList* class_definition_list) {

#ifdef ENABLE_TRACE
    printf("parser: createClassDefinitionList\\n");
#endif /* ENABLE_DUMP */

    ClassDefinitionList* ptr = _alloc_ds(ClassDefinitionList);
    initAst(&ptr->ast, NULL, AST_CLASS_DEFINITION_LIST);
    ptr->class_definition_item = class_definition_item;
    ptr->class_definition_list = class_definition_list;

    return ptr;
}

/**
 * @brief Execute pass 1 analysis for this data structure.
 *
 */
AstResult pass1ClassDefinitionList(ClassDefinitionList* ptr) {

    if(ptr != NULL) {
        // perform the pass 1 routines

        // traverse the data structure items
        if(ptr->class_definition_item != NULL) {
            AstResult res = pass1ClassDefinitionItem(ptr->class_definition_item);
            if(res != AST_RES_OK)
                return res;
        }

        if(ptr->class_definition_list != NULL) {
            AstResult res = pass1ClassDefinitionList(ptr->class_definition_list);
            if(res != AST_RES_OK)
                return res;
        }

    }
    return AST_RES_OK;
}

/**
 * @brief Execute pass 2 analysis for this data structure.
 *
 */
AstResult pass2ClassDefinitionList(ClassDefinitionList* ptr) {

    if(ptr != NULL) {
        // perform the pass 2 routines

        // traverse the data structure items
        if(ptr->class_definition_item != NULL) {
            AstResult res = pass2ClassDefinitionItem(ptr->class_definition_item);
            if(res != AST_RES_OK)
                return res;
        }

        if(ptr->class_definition_list != NULL) {
            AstResult res = pass2ClassDefinitionList(ptr->class_definition_list);
            if(res != AST_RES_OK)
                return res;
        }

    }
    return AST_RES_OK;
}

/**
 * @brief Execute pass 3 analysis for this data structure.
 *
 */
AstResult pass3ClassDefinitionList(ClassDefinitionList* ptr) {

    if(ptr != NULL) {
        // perform the pass 3 routines

        // traverse the data structure items
        if(ptr->class_definition_item != NULL) {
            AstResult res = pass3ClassDefinitionItem(ptr->class_definition_item);
            if(res != AST_RES_OK)
                return res;
        }

        if(ptr->class_definition_list != NULL) {
            AstResult res = pass3ClassDefinitionList(ptr->class_definition_list);
            if(res != AST_RES_OK)
                return res;
        }

    }
    return AST_RES_OK;
}

/**
 * @brief Emit final output for this data structure.
 *
 */
AstResult emitClassDefinitionList(ClassDefinitionList* ptr) {

    if(ptr != NULL) {
        // perform the emitter routines

        // traverse the data structure items
        if(ptr->class_definition_item != NULL) {
            AstResult res = emitClassDefinitionItem(ptr->class_definition_item);
            if(res != AST_RES_OK)
                return res;
        }

        if(ptr->class_definition_list != NULL) {
            AstResult res = emitClassDefinitionList(ptr->class_definition_list);
            if(res != AST_RES_OK)
                return res;
        }

    }
    return AST_RES_OK;
}

/**
 * @brief void destroyClassDefinitionList(ClassDefinitionList* ptr)
 *
 */
AstResult destroyClassDefinitionList(ClassDefinitionList* ptr) {

    if(ptr != NULL) {
        // destroy these data elements

        // traverse the data structure items
        if(ptr->class_definition_item != NULL) {
            AstResult res = destroyClassDefinitionItem(ptr->class_definition_item);
            if(res != AST_RES_OK)
                return res;
        }

        if(ptr->class_definition_list != NULL) {
            AstResult res = destroyClassDefinitionList(ptr->class_definition_list);
            if(res != AST_RES_OK)
                return res;
        }

        _free(ptr);
    }
    return AST_RES_OK;
}

/**
 * @brief Dump the data type and progress the tree.
 *
 */
#ifdef ENABLE_DUMP
AstResult dumpClassDefinitionList(ClassDefinitionList* ptr) {

    //(void)ptr;
    if(ptr != NULL) {
        dumpAst(&ptr->ast);
        // dump this item

        // traverse the data structure items
        if(ptr->class_definition_item != NULL) {
            AstResult res = dumpClassDefinitionItem(ptr->class_definition_item);
            if(res != AST_RES_OK)
                return res;
        }

        if(ptr->class_definition_list != NULL) {
            AstResult res = dumpClassDefinitionList(ptr->class_definition_list);
            if(res != AST_RES_OK)
                return res;
        }

    }
    return AST_RES_OK;
}
#endif /* ENABLE_DUMP */
