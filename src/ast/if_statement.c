/**
 * @file if_statement.c
 *
 * @brief $$ = createIfStatement();
 *
 * NOTE: This file is automatically generated by the boilerplate script.
 */
#include "memory.h"
#include "ast.h"

/**
 * @brief IfStatement* createIfStatement(IfClause* if_clause,
		ElseClauseList* else_clause_list)
 *
 */
IfStatement* createIfStatement(IfClause* if_clause,
		ElseClauseList* else_clause_list) {

#ifdef ENABLE_TRACE
    printf("parser: createIfStatement\\n");
#endif /* ENABLE_DUMP */

    IfStatement* ptr = _alloc_ds(IfStatement);
    initAst(&ptr->ast, NULL, AST_IF_STATEMENT);
    ptr->if_clause = if_clause;
    ptr->else_clause_list = else_clause_list;


    return ptr;
}

/**
 * @brief Execute pass 1 analysis for IfStatement data structure.
 *
 */
AstResult pass1IfStatement(IfStatement* ptr) {

#ifdef ENABLE_TRACE
    printf("parser: pass1IfStatement\\n");
#endif /* ENABLE_DUMP */

    if(ptr != NULL) {
        // perform the pass 1 routines

        // traverse the data structure items
        if(ptr->if_clause != NULL) {
            AstResult res = pass1IfClause(ptr->if_clause);
            if(res != AST_RES_OK)
                return res;
        }

        if(ptr->else_clause_list != NULL) {
            AstResult res = pass1ElseClauseList(ptr->else_clause_list);
            if(res != AST_RES_OK)
                return res;
        }

    }

    return AST_RES_OK;
}

/**
 * @brief Execute pass 2 analysis for IfStatement data structure.
 *
 */
AstResult pass2IfStatement(IfStatement* ptr) {

#ifdef ENABLE_TRACE
    printf("parser: pass2IfStatement\\n");
#endif /* ENABLE_DUMP */

    if(ptr != NULL) {
        // perform the pass 2 routines

        // traverse the data structure items
        if(ptr->if_clause != NULL) {
            AstResult res = pass2IfClause(ptr->if_clause);
            if(res != AST_RES_OK)
                return res;
        }

        if(ptr->else_clause_list != NULL) {
            AstResult res = pass2ElseClauseList(ptr->else_clause_list);
            if(res != AST_RES_OK)
                return res;
        }

    }

    return AST_RES_OK;
}

/**
 * @brief Execute pass 3 analysis for IfStatement data structure.
 *
 */
AstResult pass3IfStatement(IfStatement* ptr) {

#ifdef ENABLE_TRACE
    printf("parser: pass3IfStatement\\n");
#endif /* ENABLE_DUMP */

    if(ptr != NULL) {
        // perform the pass 3 routines

        // traverse the data structure items
        if(ptr->if_clause != NULL) {
            AstResult res = pass3IfClause(ptr->if_clause);
            if(res != AST_RES_OK)
                return res;
        }

        if(ptr->else_clause_list != NULL) {
            AstResult res = pass3ElseClauseList(ptr->else_clause_list);
            if(res != AST_RES_OK)
                return res;
        }

    }

    return AST_RES_OK;
}

/**
 * @brief Emit final output for IfStatement data structure.
 *
 */
AstResult emitIfStatement(IfStatement* ptr) {

#ifdef ENABLE_TRACE
    printf("parser: emitIfStatement\\n");
#endif /* ENABLE_DUMP */

    if(ptr != NULL) {
        // perform the emitter routines

        // traverse the data structure items
        if(ptr->if_clause != NULL) {
            AstResult res = emitIfClause(ptr->if_clause);
            if(res != AST_RES_OK)
                return res;
        }

        if(ptr->else_clause_list != NULL) {
            AstResult res = emitElseClauseList(ptr->else_clause_list);
            if(res != AST_RES_OK)
                return res;
        }

    }

    return AST_RES_OK;
}

/**
 * @brief Destroy a IfStatement data structure.
 *
 */
AstResult destroyIfStatement(IfStatement* ptr) {

#ifdef ENABLE_TRACE
    printf("parser: destroyIfStatement\\n");
#endif /* ENABLE_DUMP */

    if(ptr != NULL) {
        // destroy these data elements

        // traverse the data structure items
        if(ptr->if_clause != NULL) {
            AstResult res = destroyIfClause(ptr->if_clause);
            if(res != AST_RES_OK)
                return res;
        }

        if(ptr->else_clause_list != NULL) {
            AstResult res = destroyElseClauseList(ptr->else_clause_list);
            if(res != AST_RES_OK)
                return res;
        }

        _free(ptr);
    }

    return AST_RES_OK;
}

/**
 * @brief Dump IfStatement data type and progress the tree.
 *
 */
#ifdef ENABLE_DUMP
AstResult dumpIfStatement(IfStatement* ptr) {

    //(void)ptr;
    if(ptr != NULL) {
        dumpAst(&ptr->ast);
        // dump this item

        // traverse the data structure items
        if(ptr->if_clause != NULL) {
            AstResult res = dumpIfClause(ptr->if_clause);
            if(res != AST_RES_OK)
                return res;
        }

        if(ptr->else_clause_list != NULL) {
            AstResult res = dumpElseClauseList(ptr->else_clause_list);
            if(res != AST_RES_OK)
                return res;
        }

    }

    return AST_RES_OK;
}
#endif /* ENABLE_DUMP */
