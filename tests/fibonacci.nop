/* This takes over a minute to run in picoC. */

// note that 'in' is a keyword
num fib(num val) {

    if(n < 2) {
        return (val)
    }
    else {
        return(fib(n-1) + fib(n-2))
    }
}

num main() {

    num out = fib(35)

    print("the 35th number in the fibonacci series is {out}\n")
    return (0)
}
